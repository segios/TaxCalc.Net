using System.Collections.Generic;
using Txc.Model.Statement;

namespace Txc.Services
{
    public interface IEntityDataReader
    {
        //List<T> ReadSection<T>(StatementData dataContainer) where T : new();
        //List<T> ReadSection<T>(StatementData dataContainer, string section,
        //   IDictionary<string, string> fieldMapper) where T : new();
    }
}
